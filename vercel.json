
{
  "version": 2,
  "builds": [
    { "src": "package.json", "use": "@vercel/static-build" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "https://traning-center.runasp.net/api/$1" },
    { "src": "/(.*)", "dest": "/" }
  ],
  "buildCommand": "npm run build",
  "outputDirectory": "dist"
}
